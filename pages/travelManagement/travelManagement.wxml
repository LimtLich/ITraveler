<!--index.wxml-->
<view class="container">
  <view class="Travels" wx:for="{{travels}}" wx:key="id">    
    <image src="{{item.cover_img}}" mode="aspectFill"></image>
    <p class='Travels_title'>{{item.title}}</p>
    <p class='Travels_date'>{{item.date}}</p>
  </view>
</view>